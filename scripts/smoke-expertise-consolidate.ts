/**
 * Gate for spec 010-expertise-consolidate.
 *
 * RED stub — exits 1 until scripts/expertise-consolidate.ts and its pure fns
 * land. Replaced in task 4 with a real fixture harness (`mkdtempSync` tmpdir
 * with known duplicate + stale entries, run the consolidator, assert kept /
 * dropped counts + idempotency).
 */

console.error("smoke-expertise-consolidate: not yet implemented");
process.exit(1);
