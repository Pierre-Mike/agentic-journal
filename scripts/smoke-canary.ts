/**
 * Gate for spec 012-canary-skill-suite.
 *
 * RED stub. Will be replaced in task 6 with an end-to-end smoke that builds a
 * tmpdir baseline via mkdtempSync, invokes canary-run.ts against it, and
 * asserts the framework scores correctly.
 */

console.error("smoke-canary.ts: not yet implemented (RED)");
process.exit(1);
