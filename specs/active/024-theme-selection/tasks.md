# Tasks

- [ ] 1. Author RED gate + unit shells — write `e2e/theme.e2e.ts` and `src/lib/theme.test.ts` in failing form (imports from `src/lib/theme.ts` which does not exist; DOM assertions will fail because the theme system is not wired)
  - agent: main
  - depends: []
  - file_targets: [e2e/theme.e2e.ts, src/lib/theme.test.ts]
  - boundary: [e2e/theme.e2e.ts, src/lib/theme.test.ts]
- [ ] 2. Implement `src/lib/theme.ts` with `resolveTheme(stored, prefersDark)` and `nextTheme(current)` as pure functions returning `"light" | "dark"` — unit test goes green
  - agent: main
  - depends: [1]
  - file_targets: [src/lib/theme.ts]
  - boundary: [src/lib/theme.ts]
- [ ] 3. Author `src/components/ThemeToggle.astro` — button with `aria-label`, Unicode glyph, sr-only state span, and client script that imports `nextTheme` and mutates `data-theme` + `localStorage.theme` on click
  - agent: main
  - depends: [2]
  - file_targets: [src/components/ThemeToggle.astro]
  - boundary: [src/components/ThemeToggle.astro]
- [ ] 4. Wire `src/layouts/BaseLayout.astro` — add `data-theme="light"` on `<html>`, `[data-theme="dark"]` CSS variable block (including code/pre backgrounds), inline synchronous head script resolving theme before body paint, and render `<ThemeToggle/>` in the header
  - agent: main
  - depends: [3]
  - file_targets: [src/layouts/BaseLayout.astro]
  - boundary: [src/layouts/BaseLayout.astro]
- [ ] 5. Drive `e2e/theme.e2e.ts` green — run Playwright; fix any timing, selector, or computed-style mismatches discovered across earlier files. Allow loopback into any touched file.
  - agent: main
  - depends: [4]
  - file_targets: [e2e/theme.e2e.ts, src/layouts/BaseLayout.astro, src/components/ThemeToggle.astro, src/lib/theme.ts]
  - boundary: [e2e/theme.e2e.ts, src/layouts/BaseLayout.astro, src/components/ThemeToggle.astro, src/lib/theme.ts]
